### output
[
	{
		"num": "one"
	},
	{
		"num": "two"
	},
	[
		"C"
	],
	[
		"A"
	]
]
###

enum Numbers -> string:
	one; two; three;

def TakeNumber(num: Numbers) -> dict:
	num: num;

TakeNumber(one);
TakeNumber(two);

enum letters -> string:
	A; B; C;

def TakeLetter(letter: letters) -> list:
	letter;

TakeLetter(C);
TakeLetter(A);