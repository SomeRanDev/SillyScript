def myFunc(num: int) -> dict:
	def inner(name: string, kind: int) -> dict:
		name: name;
		kind: kind;
		moreData: false;

	something: inner("Hello", 1);
	somethingElse: inner("Goodbye", 2);

myFunc(1);

###
syntax If:
	pattern:
		if <condition: dict!condition>:
			<contents: dict!action list>

	## Transmutes ##
	def transmute(condition: dict, contents: list) -> dict!action:
		type: 2;
		condition: condition;
		if_true: contents;
		if_false: null;
###
