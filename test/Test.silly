# BLabdlsakh

def CameraMove(start: int, end: int) -> dict!abc:
	type: 0;
	start: start;
	end: end;
# fdsfsdf
def StartAnimation(nodePath: string, animationName: string) -> dict!abc:
	type: 1;
	nodePath: nodePath;
	animationName: animationName;

syntax If:
	pattern -> int dict:
		if <condition: dict!bool>:
			<things: dict!abc list>

syntax Compare:
	pattern -> dict!bool:
		<first: int> == <second: int>
# fdfwef 
if 12 == 321:
	CameraMove(1, 2);
	StartAnimation("../Player", "Intro");
	CameraMove(3, 5);

# gregrge
syntax Increment:
		pattern:
			<number: int>--

:
	syntax Increment:
		pattern:
			<number: int>++

	123++;

123--;

def Bla(a: int, b: int) -> dict:
	a: a;
	b: b;

def Bla2(a: int, b: int) -> list:
	a; b;

Bla(321, a: 123);
Bla2(321, a: 333);

# dfsgrwe 
# gerger

syntax Dead:
	pattern:
		💀<data: any>💀

def GetNum() -> int list:
	123;

💀321💀;
💀123💀;
💀GetNum()💀;

enum RPGMaker -> string:
	mv;
	mz;

def Blabla(a: RPGMaker = mz) -> dict:
	a: a;

Blabla();


